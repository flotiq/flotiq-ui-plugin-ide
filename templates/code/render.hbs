const { contentType, {{fieldKey}} } = flotiqEvent;

// Don't override fields other than 'title'
if (contentType?.name !== "blogposts" || {{fieldKey}} !== 'title') return null;

const cacheKey = `${pluginInfo.id}-${contentType.name}-{{asTemplateStringVar fieldKey}}`;
let element = getCachedElement(cacheKey)?.element;

if (!element) {
  element = document.createElement('div');
  // do your magic!
}

addElementToCache(element, cacheKey);

return element;
